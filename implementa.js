"use strict";
var hospital;
(function (hospital) {
    let h = new hospital.Hospital;
    h.setNomeHospital("Hospita");
    h.addMedico;
    h.addMedico;
    h.addMedico;
    h.addPaciente;
    h.addPaciente;
    h.addPaciente;
})(hospital || (hospital = {}));
